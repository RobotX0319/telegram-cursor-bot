# Cloud Agent qoidalari — telegram-video-bot/

Bu papka **bitta adminning video bot loyihasi**. Agent faqat shu yerda ishlaydi.

## Ruxsat

- `telegram-video-bot/` ichidagi barcha fayllar

## Taqiq

- `src/` (telegram-cursor-bot platformasi)
- `scripts/` (ildiz)
- `ish/` (boshqa admin)
- `wrangler.jsonc` (ildiz)

## Oqim

1. Kod o'zgartirish — faqat `telegram-video-bot/` ichida
2. `git commit` + `git push origin main`
3. GitHub Actions avtomatik deploy qiladi

## Eslatma

Agar papka hali yaratilmagan bo'lsa, avval papka yarating. Kod yozishni boshlamang.
